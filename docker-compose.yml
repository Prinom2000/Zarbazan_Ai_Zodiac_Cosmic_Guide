services:
  zarbazan_ai:
    build: .     
    image: softvence/zarbazan_ai:latest  
    container_name: zarbazan_ai_container 
    ports:
      - "8001:8000"               
    env_file:
      - .env                          
    restart: always                  